mutation MyMutation($id: ID, $studentRequest: StudentRequest) {
    updateStudent(id: $id, studentRequest: $studentRequest ){
        id
        email
        fullName
        firstName
        lastName
    }
}