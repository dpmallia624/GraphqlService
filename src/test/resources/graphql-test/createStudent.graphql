mutation MyMutation($studentRequest: StudentRequest) {
    createStudent(
        studentRequest: $studentRequest
    ) {
        id
        firstName
        lastName
        email
        addressResponse {
            city
            street
        }
    }
}